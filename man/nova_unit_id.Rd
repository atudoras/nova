% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dynamics_utils.R
\name{nova_unit_id}
\alias{nova_unit_id}
\title{Collapse well identity into a single label}
\usage{
nova_unit_id(data, cols = NULL)
}
\arguments{
\item{data}{A data frame carrying MEA data.}

\item{cols}{Identity columns; defaults to [nova_unit_cols()].}
}
\value{
Character vector of one label per row, or `NULL` if `data` carries no
  identity columns.
}
\description{
Use for display, row names and counting. Where a grouping has to be exact,
group on the columns themselves ([nova_unit_cols()]) rather than on this
string: two identity columns pasted together can in principle collide if the
values contain the separator.
}
\examples{
d <- data.frame(Experiment = c("MEA1", "MEA2"), Well = c("A1", "A1"))
nova_unit_id(d)
}
\seealso{
[nova_unit_cols()]
}
