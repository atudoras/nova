% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dynamics_landscape.R
\name{nova_landscape}
\alias{nova_landscape}
\title{State-occupancy landscape}
\usage{
nova_landscape(
  x,
  dims = c("PC1", "PC2"),
  group_var = NULL,
  timepoint_var = "Timepoint",
  timepoint_order = NULL,
  n_grid = 120,
  overlay_trajectories = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{x}{A PCA result, embedding data frame, or \code{nova_trajectories}.
Density uses ALL samples in \code{plot_data} (not just group means) when a
raw embedding is supplied.}

\item{dims}{Embedding columns (default \code{c("PC1","PC2")}).}

\item{group_var, timepoint_var, timepoint_order}{Extraction args (for the
trajectory overlay).}

\item{n_grid}{Grid resolution per axis (default 120).}

\item{overlay_trajectories}{Logical; draw mean group trajectories on top
(default \code{TRUE}).}

\item{verbose}{Logical.}
}
\value{
Object of class \code{nova_landscape} with \code{density} (grid),
  \code{grid} (long df), and \code{plots} (\code{density}, \code{potential},
  \code{occupancy}).
}
\description{
Estimates and plots the density of occupied states in the embedding plane -
the regions neuronal networks visit most - plus a pseudo-potential surface
\eqn{U = -\log p} (valleys = frequently occupied / attractor-like) and an
optional overlay of the mean trajectories.
}
\examples{
set.seed(1)
df <- data.frame(
  PC1 = c(rnorm(60), rnorm(60, 4)), PC2 = c(rnorm(60), rnorm(60, 3)),
  Treatment = rep(c("A","B"), each = 60),
  Timepoint = rep(c("baseline","30min","1h","2h"), 30)
)
L <- nova_landscape(df, overlay_trajectories = FALSE, verbose = FALSE)
}
