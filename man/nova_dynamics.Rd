% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dynamics_describe.R
\name{nova_dynamics}
\alias{nova_dynamics}
\title{Run the full NOVA dynamics pipeline}
\usage{
nova_dynamics(
  x,
  analyses = c("geometry", "regime", "transitions", "similarity", "landscape"),
  dims = c("PC1", "PC2"),
  group_var = NULL,
  unit_var = NULL,
  timepoint_var = "Timepoint",
  timepoint_order = NULL,
  similarity_method = "dtw",
  k = NULL,
  verbose = TRUE
)
}
\arguments{
\item{x}{A PCA result or embedding data frame.}

\item{analyses}{Character vector; any of \code{"geometry"}, \code{"regime"},
\code{"transitions"}, \code{"similarity"}, \code{"landscape"}
(default: all).}

\item{dims, group_var, unit_var, timepoint_var, timepoint_order}{Extraction args.}

\item{similarity_method}{Method for \code{nova_trajectory_similarity()}.}

\item{k}{Number of states for \code{nova_transition_matrix()}.}

\item{verbose}{Logical.}
}
\value{
Object of class \code{nova_dynamics} holding the sub-results in
  \code{$geometry}, \code{$regime}, \code{$transitions}, \code{$similarity},
  \code{$landscape}, plus the shared \code{$trajectories}.
}
\description{
Convenience wrapper that extracts trajectories once and runs the requested
dynamics analyses, returning a single object that \code{nova_describe()} can
narrate end-to-end.
}
\examples{
df <- data.frame(
  PC1 = c(0,2,3,3.2, 0,1,0,1), PC2 = c(0,0,0,0, 0,1,0,1),
  Treatment = rep(c("A","B"), each = 4),
  Timepoint = rep(c("baseline","30min","1h","2h"), 2))
dyn <- nova_dynamics(df, analyses = c("geometry","regime"),
                     group_var = "Treatment", verbose = FALSE)
}
