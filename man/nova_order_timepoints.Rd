% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dynamics_utils.R
\name{nova_order_timepoints}
\alias{nova_order_timepoints}
\title{Order timepoint labels into a biologically correct sequence}
\usage{
nova_order_timepoints(timepoints, baseline_first = TRUE)
}
\arguments{
\item{timepoints}{Character/factor vector of timepoint labels (duplicates allowed).}

\item{baseline_first}{Logical; force baseline-like labels to the front (default \code{TRUE}).}
}
\value{
Character vector of unique labels in dynamical order.
}
\description{
Produces an ordering in which baseline-like labels always come first, the
remaining labels are sorted by parsed real time (minutes), and any genuinely
unparseable labels are appended alphabetically. This is the canonical
replacement for hardcoded timepoint lists: it correctly orders compound
labels such as \code{"1h15"} / \code{"1h30"} / \code{"1h45"} that naive
alphabetical sorting mis-ranks.
}
\examples{
nova_order_timepoints(c("1h30", "baseline", "15min", "1h", "2h", "0min", "1h15"))
}
